import React from "react";
import { BrowserRouter } from "react-router-dom";
const Games = () => {
    return (
        <BrowserRouter>
            <div></div>
        </BrowserRouter>
    );
};

export default Games;
